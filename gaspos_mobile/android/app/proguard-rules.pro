# Flutter registers plugins through generated Java/Kotlin code. Keep only the
# runtime metadata required by serialization and Android framework callbacks.
-keepattributes Signature,InnerClasses,EnclosingMethod
-dontwarn javax.annotation.**
